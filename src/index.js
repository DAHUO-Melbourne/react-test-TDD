import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

/**
 * TDD与BDD的区别
 * 1. TDD是先写测试用例再根据测试用例写代码
 * 2. BDD是先写代码再写测试用例
 */